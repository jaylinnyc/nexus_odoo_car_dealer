<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="action_migrate_financing_data" model="ir.actions.server">
            <field name="name">Migrate Financing Data</field>
            <field name="model_id" ref="model_financing_agreement"/>
            <field name="state">code</field>
            <field name="code">
count = model.migrate_old_data()
action = {
    'type': 'ir.actions.client',
    'tag': 'display_notification',
    'params': {
        'title': 'Migration Complete',
        'message': 'Successfully migrated %s vehicles to new financing structure.' % count,
        'type': 'success',
        'sticky': False,
    }
}
            </field>
        </record>

        <!-- Menu item to run migration -->
        <menuitem id="menu_migrate_financing"
                  name="Run Migration Script"
                  parent="menu_financing_root"
                  action="action_migrate_financing_data"
                  sequence="99"/>
    </data>
</odoo>
